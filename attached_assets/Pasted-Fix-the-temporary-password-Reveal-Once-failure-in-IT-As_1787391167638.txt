Fix the temporary password Reveal Once failure in IT-Asset-Hub.

CURRENT STATE

Windows User Push is now working successfully.

Device:
- MILES-LAP-882
- Windows 11
- Agent 0.9.9
- Device Online
- Employee Code: MPI545
- OS username: mpi545
- Account successfully created
- Account is enabled
- Account is a standard user
- Account is NOT in Administrators

The portal shows:

User Provisioned

But when I click:

Reveal Once

I get:

"Temporary password unavailable"
"Failed to send a request to the Edge Function"

IMPORTANT:
Do NOT recreate the Windows user.
Do NOT re-run User Push.
Do NOT regenerate the employee password unless the existing credential is truly unavailable and you explain why.
Do NOT change the Windows agent unless necessary.
Do NOT expose any secret values.

GOAL

Make the existing Reveal Once flow work so an authorized IT admin can retrieve the already-prepared temporary password exactly once.

TRACE THIS PATH END TO END:

Employee OS Account card
-> Reveal Once button
-> frontend request
-> Supabase Edge Function
-> provisioning-credentials function
-> reveal_provisioning_credential RPC
-> encrypted credential retrieval
-> decryption
-> one-time consume
-> plaintext returned only to authenticated authorized portal user

CHECK ALL OF THESE:

1. Frontend
- Find the exact function call used by Reveal Once.
- Confirm function name and URL are correct.
- Confirm it points to the correct Supabase project.
- Confirm required Authorization header/session token is sent.
- Confirm request body matches the Edge Function contract.
- Confirm CORS handling is valid.

2. Edge Function deployment
Inspect:

artifacts/asset-desk/supabase/functions/provisioning-credentials/index.ts

Confirm whether the live Supabase project actually has the latest version deployed.

If not deployed, clearly say so.

3. Secret configuration
Confirm existence only, never print values:

PROVISIONING_CREDENTIAL_KEY

Also confirm normal Supabase secrets required by the function are available.

Both:
- agent-api
- provisioning-credentials

must use the same PROVISIONING_CREDENTIAL_KEY.

Do not reveal the secret value.

4. RPC/database
Verify the live database has:

public.reveal_provisioning_credential(...)

Verify:
- super_admin and it_admin are allowed
- unauthorized users are denied
- credential is consumed atomically
- expired credentials cannot be revealed
- second reveal fails

5. Existing credential state
For MILES-LAP-882 / mpi545, inspect only metadata/status.

Do not print ciphertext or plaintext.

Confirm whether its credential state is:
- prepared
- available
- consumed
- expired
- revoked
- missing

If it is still available, preserve it and fix the reveal path.

6. Error source
Find the exact reason for:

"Failed to send a request to the Edge Function"

Determine whether it is:
- function not deployed
- wrong function URL
- CORS failure
- auth/JWT failure
- missing secret
- Edge Function crash
- incorrect request body
- RPC failure
- network/region issue

Do not replace the error with a friendlier message without fixing the real cause.

7. Live fix authorization
I authorize you to:
- deploy the corrected provisioning-credentials Edge Function if required
- deploy agent-api only if required for this reveal flow
- configure missing PROVISIONING_CREDENTIAL_KEY secret if it is already available in the project environment/configuration

Do NOT invent a new secret.
Do NOT print secret values.
Do NOT change unrelated production data.

8. Validation
After fixing:
- call the Edge Function using an authenticated test path without revealing the real password in logs/output
- verify HTTP response path works
- verify authorized role reaches the reveal RPC
- stop BEFORE consuming the actual MPI545 credential if a non-consuming validation is possible

If the only valid live test consumes the credential, stop and tell me:
READY FOR ONE-TIME REVEAL TEST: YES

Then I will click Reveal Once manually.

FINAL RESPONSE FORMAT

ROOT CAUSE:
FRONTEND CHANGE:
EDGE FUNCTION STATUS:
EDGE FUNCTION DEPLOYED:
SECRET CONFIGURATION:
LIVE RPC STATUS:
MPI545 CREDENTIAL STATUS:
CORS/AUTH STATUS:
FILES CHANGED:
TEST RESULTS:
READY FOR ONE-TIME REVEAL TEST: YES/NO

If you cannot complete the live fix, say exactly:

BLOCKED — REVEAL PATH NOT FIXED